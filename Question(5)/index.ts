// Storing the person's name with whitespace characters
let whitespaceName: string = "\t \n Usama \n\t";

// Printing the name with whitespace displayed
console.log(whitespaceName);

// Stripping the whitespace characters and printing the cleaned name
let cleanedName: string = whitespaceName.trim();
console.log(cleanedName);
